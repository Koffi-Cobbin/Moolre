Django>=5.0,<5.3
djangorestframework>=3.15
drf-spectacular>=0.27
django-environ>=0.11
# NOTE: the original "django-cryptography" package on PyPI (v1.1) is
# abandoned and breaks on Django 5+ (imports django.utils.baseconv, which
# Django removed in 5.0 — confirmed via github.com/georgemarshall/
# django-cryptography/issues/115). Use this maintained fork instead:
django-cryptography-5>=2.0.3
httpx>=0.27
tenacity>=8.2
psycopg2-binary>=2.9
responses>=0.25
python-dateutil>=2.9

# v2 (not needed for v1, listed here for reference — see plan Section 7 & 14)
# celery>=5.4
# redis>=5.0
